server:
  error:
    include-message: always
    include-binding-errors: always
  port: 4000
  servlet:
    context-path: /api

logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.validation: DEBUG

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lbs?serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: lbsuser
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true

logging.level.org.hibernate.SQL: debug


kakao:
  rest-key: ${KAKAO_REST_KEY}